# Intentionally kept empty so offline CI/build environments can run
# `python -m pip install -r requirements.txt` without contacting PyPI.
# Use `requirements-runtime.txt` when installing the actual bot runtime.
